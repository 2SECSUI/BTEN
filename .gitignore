# Generated Move and local runtime material
build/
.move/
runtime/
*.log
node_modules/

# Never commit credentials, keys, recovery material, or sponsor authorization data
*.env
*.env.*
!*.env.template
*.keystore
secrets/

# Monitoring snapshots are generated locally; publish reviewed snapshots only.
monitoring/live/

tools/

composable_test_out.txt

# Turbos full upstream snapshot (optional local only)
.turbos-sui-move-interface/clmm/sources.bak/

artifacts/v17/package_dump.json
artifacts/v17/build-dump.json
